# Generated drawings
outputs/
# User-uploaded templates (large binaries; track templates separately if needed)
uploads/
# Local-only config overrides (DB creds, server names, etc.)
config/config.local.php
# Editor / OS noise
.vscode/
.DS_Store
Thumbs.db
*.log
# Email / reference attachments dropped in the repo root
*.zip
*.eml
*.msg
# Temp / build
*.tmp
*.bak
*.err
