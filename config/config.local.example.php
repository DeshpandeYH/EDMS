<?php
/**
 * Local development overrides for EDMS.
 *
 * Copy this file to config/config.local.php and edit. It is gitignored.
 * Anything you define here wins over config/database.php defaults.
 *
 * Common overrides:
 *   - Use Windows auth against SQLEXPRESS on a dev box
 *   - Point at a different server / database / credentials
 *   - Move uploads/outputs to a different drive
 */

// Example: local dev on SQLEXPRESS with Windows auth.
// define('DB_SERVER', 'localhost\\SQLEXPRESS');
// define('DB_USE_WINDOWS_AUTH', true);

// Example: alternate password for sa on production.
// define('DB_PASS', 'YourRealPasswordHere');
